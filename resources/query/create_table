CREATE TABLE %s (id INT NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY, date DATE, title VARCHAR(128), text VARCHAR(32672), mood INTEGER DEFAULT 5 NOT NULL, desire INTEGER DEFAULT 5 NOT NULL, blood BOOLEAN NOT NULL, tears BOOLEAN NOT NULL)
